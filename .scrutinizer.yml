filter:
    excluded_paths: [vendor/*, tests/*, lib/Middleware/DigestAuthMiddleware.php]
    
checks:
    php:
        code_rating: true
        duplication: true
tools:
    external_code_coverage: true
